.env
*.env
.env.*
!.env.example
__pycache__/
*.pyc
.DS_Store
# Ignore local Claude Code state, but DO version the shared build-time agent
# config (subagents, loop commands, the canonical sensitive-path list) so the
# model-routing setup is identical for everyone on the team.
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/sensitive-paths.md
fail_logs.txt
test_output.log
